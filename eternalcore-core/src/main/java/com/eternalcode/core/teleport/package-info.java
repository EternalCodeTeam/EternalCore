/**
 * Shared teleport engine used by many features (home, back, warp, spawn, rtp, teleport requests).
 * It's too common to move it inside {@code feature} package
 */
package com.eternalcode.core.teleport;
